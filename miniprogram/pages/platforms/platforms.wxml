<view class="page">
  <view class="hero-card">
    <view class="eyebrow">算法的观察窗口</view>
    <view class="title">没有一个平台看得到完整的你。</view>
    <view class="desc">每个平台只能从自己的场景里看到一部分行为痕迹。多完成一个视角，数字分身就多出现一块彩色投影。</view>
  </view>

  <view class="merge-card" bindtap="goMerge">
    <view>
      <view class="merge-kicker">数字分身</view>
      <view class="merge-title">已解锁 {{completedCount}} / {{platforms.length}} 个观察视角</view>
      <view class="merge-desc">不是完成度，也不是人格测试。这里只表示算法从多少种生活场景观察过你。</view>
    </view>
    <view class="merge-arrow">→</view>
  </view>

  <view class="section-title">选择一个算法观察你的窗口</view>

  <view wx:for="{{platforms}}" wx:key="id" class="platform-card" data-id="{{item.id}}" bindtap="openPlatform">
    <view class="platform-top">
      <view class="platform-icon" style="background: {{item.color}}; color: {{item.accent}};">{{item.icon}}</view>
      <view class="platform-main">
        <view class="platform-head">
          <view>
            <view class="platform-name">{{item.name}}</view>
            <view class="view-name">{{item.viewName}}</view>
          </view>
          <view wx:if="{{item.completed}}" class="done-badge">已观察</view>
        </view>
        <view class="platform-subtitle">{{item.subtitle}}</view>
        <view class="observe-chips">
          <text wx:for="{{item.observes}}" wx:key="*this">{{item}}</text>
        </view>
        <view wx:if="{{item.completed}}" class="result-preview">已形成 {{item.inferenceCount}} 条概率推断 · {{item.observedCount}} 个行为侧面有足够证据</view>
      </view>
      <view class="arrow">›</view>
    </view>
  </view>

  <view class="merge-explain">
    <view class="merge-explain-title">规则只有一条：看不到的，就不猜。</view>
    <view class="merge-explain-text">一个答案不会直接变成一个“人格标签”。只有多条行为痕迹指向同一方向时，系统才会生成推断；证据不足的部分会保留为空白。</view>
  </view>

  <view class="privacy-note">本项目模拟平台如何从外在行为痕迹做概率推演，不读取真实平台后台标签，不代表任何平台的真实用户画像。</view>
</view>
